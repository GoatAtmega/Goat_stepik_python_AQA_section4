[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    win10
    beta_users
    without_localization: test without localization, don't use option --language
    login_guest: test without authorization
    login: test with authorization
    need_review: mark for tusk